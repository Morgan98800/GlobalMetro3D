{
  "name": "@paris-subway/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run --root .. tests/shapes.test.ts tests/paris_time.test.ts",
    "test:train-models": "vitest run --root .. tests/train_models_layer.test.ts",
    "test:shapes": "vitest run --root .. tests/shapes.test.ts",
    "test:time": "vitest run --root .. tests/paris_time.test.ts"
  },
  "dependencies": {
    "@deck.gl/core": "^9.1.4",
    "@deck.gl/extensions": "^9.1.4",
    "@deck.gl/geo-layers": "^9.1.4",
    "@deck.gl/layers": "^9.1.4",
    "@deck.gl/mapbox": "^9.1.4",
    "@deck.gl/mesh-layers": "^9.4.0",
    "@loaders.gl/gltf": "^4.5.1",
    "@luma.gl/gltf": "^9.4.0",
    "@paris-subway/shared": "*",
    "maplibre-gl": "^5.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
